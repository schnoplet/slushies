# Weather App

A simple Flask weather app that uses your browser location to fetch real-time weather data.

## Features
- Uses browser geolocation
- Fetches live weather data from Open-Meteo API
- Displays temperature and wind speed

## Tech
- Flask
- Open-Meteo API
- HTML/CSS/JS

## How to run
pip install -r requirements.txt
python app.py